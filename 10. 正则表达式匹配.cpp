class Solution {
public:
    bool isMatch(string s, string p) {
        int m = s.size();
        int n = p.size();

        if (n == 0) {
            return false;
        }

        if (m == 0) {
            return true;
        }

        for (char c : s) {
            if ()
        }
    }
};